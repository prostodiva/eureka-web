function Hero() {
    return (
        <section aria-labelledby="hero-heading">
            <h2 id="hero-heading">Section Hero</h2>
            <p>blah-blah</p>
        </section>
    );
}

export default Hero;
